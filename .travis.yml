language: python2

sudo: required

services:
    - docker

before_install:
    - docker build -t "qe:latest-debian-atlas-mpich" .

script:
    - docker run -it "qe:latest-debian-atlas-mpich" --entrypoint simple-test.py

